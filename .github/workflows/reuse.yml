---
name: REUSE

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  license-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: reuse lint
        uses: fsfe/reuse-action@v1